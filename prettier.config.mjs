const config = {
  printWidth: 120,
  semi: false,
  singleQuote: true,
  bracketSpacing: true,
  bracketSameLine: true,
  arrowParens: 'avoid',
  plugins: ['prettier-plugin-tailwindcss'],
}

export default config
